# Decisions

| ID | Decision | Status | Date | Source/Rationale |
|----|----------|--------|------|-----------------|
| D1 | Rename `learning-hub` → `home-base` and evolve into Kyle's daily home base (morning brief + Learning Hub as the learning section) | Approved | 2026-07-13 | Kickoff contract `docs/KICKOFF-home-base.md`; `README.md` |
| D2 | The hub is strictly read-only toward NotebookLM sidecars; generating new series stays in the `audio-series` skill | Approved | 2026-06 (SPEC) | `SPEC.md`, `README.md`; enforced by the `guard-sidecars` hook + `backend/tests/test_no_sidecar_writes.py` |
| D3 | Progress data (attempts, mastery, streaks, notes, custom topics) lives in the hub's own SQLite store, kept out of the sidecars | Approved | 2026-06 (SPEC) | `README.md` architecture table |
| D4 | Deliberate gate overrides: build M3–M7 ahead of the M0 sweep-quality verdict; vindicated when M0 closed PASS with zero fabrications | Approved | 2026-07-15 → 2026-07-19 | `CLAUDE.md` status header; `docs/M0-sweep-grades.md` |
| D5 | Study-scheduler note box: local deterministic parser is the primary engine; `claude -p` only as fallback for unparseable phrasing; honest no-op message if neither reads it | Approved | 2026-07-22 | Kyle's call after the v1 note box silently did nothing (PR #141; `docs/MASTER_PLAN.md`) |
| D6 | Learning Paths build on the audio overview season as the spine; video series is supplementary only ("design decision 12"), guarded by `test_paths_fixture.py` | Approved | 2026-07-22 | PR #143 fix after the Jacobian path was wrongly built on the video season (`docs/MASTER_PLAN.md`) |
| D7 | Park Agent Gate until after the ~08-03 v1 check; the other three 07-26 moonshots stay undecided (each keeps its own future gate conversation). Demand-side finding recorded: Claude Code crons/sessions are a sufficient first tenant — the park is timing, not demand. The vision doc's three open questions stay deliberately unanswered until the revisit | Approved | 2026-07-27 | Gate conversation (Overnight-gate model); `docs/ideas/agent-gate.md` Decisions section |
| D8 | **Free-Inference Rebuild — GO** on the graded bake-off (not on a read surface). Recorded scope: (1) **ids-only, never prose** — the model emits item ids + ordering, never text, so fabrication is structurally impossible rather than detected; (2) **Ollama + a small instruct model** first, behind the existing `chat.py` `Runner` seam; (3) the reshaped view lives **beside** the fixed brief, and default status must be earned via its own graded record + gate conversation. Invariants: offline degrades to the fixed brief; assumption 2 stays uncrossed until a graded week passes. The ~08-03 park rationale explicitly does **not** transfer — the wedge has zero user-facing surface and cannot perturb the v1 measurement | Approved | 2026-07-27 | Gate conversation (Overnight-gate model); `docs/ideas/free-inference-rebuild.md` Decisions section; plan in `docs/LOCAL_READER_BAKEOFF_PLAN.md` |
| D9 | **Park The Session Note** until `brief_notes` has real depth (~6 months of accrual, so roughly 2027-01). The idea's stated "why now" was **false**: M2 shipped 2026-07-14, so at the gate the corpus was 13 days (~5–6 notes), not "months deep" — corrected in the doc. Two findings recorded for the revisit: `mirror.py`'s `MIN_SIGNAL = 5` is the project's own written refusal to describe Kyle from a handful of rows, so the Session Note needs an explicit signal floor of its own; and the exact-substring verifier proves the quotes but not the *interpretations*, which are the trust-fatal half. Open questions 1–3 stay deliberately unanswered as the revisit agenda | Approved | 2026-07-27 | Gate conversation; `docs/ideas/the-session-note.md` Decisions section |
| D11 | **Research-portfolio study path: supersede, soft gates, honest video/mindmap kinds.** The interview-prep notebook's hub path (442368c1…) — broken by the 2026-08-01 refresh (3 deleted study-aid ids) and S1-only against 32 artifacts — is **superseded** by a hand-authored 37-step phased path covering all 32 (Orient → S1 lap → Grilling I → S2 lap → Debate/Critique/Grilling II → drill) rather than repaired; per-episode quizzes are **soft gates** (6 of 8, honor-system, stated in each quiz step body — not player-enforced); the player gains `video`/`mindmap` step kinds so supplementary artifacts carry their real kind + artifact_type (the honest form of D6's audio-primary convention, not the #143 inversion). Path data is gitignored user data: validated type-aware against the hub catalog (32/32 — ids resolve, catalog types match step kinds) instead of reviewed as diff | Approved | 2026-08-02 | Kyle's picks (structure + quiz role) from three proposed approaches; PR #173 |
| D10 | **Park The Correspondence** on a demand-side finding: the human-curated wire it describes **already runs** (Marlee sends Kyle links; the vault's `marlee-sent` skill captures them), so the thesis is confirmed by observed behavior — but **no peer will run a node**, making v1 a protocol for a network of one whose loopback proof would test the plumbing and not the bet. Constraint recorded: the tempting no-protocol re-scope (a Dispatches strip off the existing capture path) reads from the vault, which the Overnight v0 gate already ruled out pending its own parked bridge — so it trades one gate for another. Questions 2–3 stay deliberately unanswered; question 1 keeps the finding rather than an answer | Approved | 2026-07-27 | Gate conversation; `docs/ideas/the-correspondence.md` Decisions section |
| D12 | **v1 success-criteria check — ruled NOT YET; extended to the ~08-19 re-grade on the phone number.** At the ~08-03 check (exactly 3 weeks post-kickoff), the only attribution-clean week — 07-27, also Kyle's away week — failed every measurable bar (3 raw / 2 phone mornings vs ≥5 · 2 notes vs ≥3), while the two raw-passing weeks (5 and 5 mornings) predate PR #159's source attribution and ride July's build blitz (17 visits on 07-16, 18 on 07-21) — certifying them would stamp build exhaust as a reading habit, the exact failure `docs/ideas/builder-vs-reader-metric.md` predicted. Verdict: neither PASS nor FAIL — **two attributed weeks (08-03 → 08-16), verdict lands at the ~08-19 re-grade**, collapsing the two gates into one. **The certifying number is `mornings_phone` ≥5** (reader-Kyle) — error model recorded both ways: desktop-over-tailnet reads over-count as `phone`, while reads of the deployed build on the Mac file as `mac-localhost` and certify as **zero** (07-28's six real opens count for nothing) — an accepted under-count; phone-only certifies, per Kyle at review. Notes bar unchanged at ≥3/week, a saved chat answer counting once, never as note *and* question. The two testimony criteria (significant events reach Kyle from elsewhere first ≤1/week · foraging → ~zero) were **not answered** at the check — Kyle tallies both live over the extended window instead of reconstructing them. The moonshot-lane freeze and the Agent Gate park (D7) hold until the extended verdict. **Supply side NOT clean** (adversarial review F1 — the original "21/21 days" counted day-directories, not briefs): 18/21 mornings rendered a brief; 07-24, 07-31, 08-01 rendered nothing (all-8-topic API errors, zero `.runs.jsonl` rows), the latter two inside the graded week. On those mornings Today served the prior day's brief under the stale marker, and a phone open still counts — visits stamp the visit day, not the served brief's date (review F8) — so the week's metric ceiling stayed 7; what was lost was freshness: only 5 of its 7 mornings offered a new brief to read. Amendment at review (Kyle's intent from the gate — an outage never counts against the habit — in F8's two-step wording): at the ~08-19 verdict, (1) count readable mornings from the rendered-brief census (`brief_runs_summary` missing days) first; (2) a week with <5 readable mornings **and** `mornings_phone` <5 is a supply failure that extends the window, never a habit failure — while a week with `mornings_phone` ≥5 despite outages has met the bar on its own terms, and Kyle decides at the verdict whether to bank it. Accepted risk (review F7): `brief_notes` carries no source attribution, so the notes bar has no build-exhaust guard — standing convention: no session/agent writes notes against the live server before the ~08-19 verdict | Approved | 2026-08-03 | Kyle's ruling at the check (this session); metrics computed read-only from the store, mirroring `brief_habit_weeks` semantics; supply + error-model corrections from adversarial review round 1 (PR #174) |
